Процесс — экземпляр программы во время выполнения, независимый объект,
которому выделены системные ресурсы (например, процессорное время и память).

Поток — определенный способ выполнения процесса.


- зачем это нам нужно (многопоточность)?
- потоки и ядра процессора + тактовая частота
- создание потока с помощью Thread
- поток main.
- порядок выполнения потоков по умолчанию
- join метод
- мысль о том что метод main = run
- важно где выполняется код (прокинем объект в поток новый)

- thread states
- пример с Thread sleep

- что такое монитор (воспринимать как флаг)
- у каждого объекта может быть тоько один монитор
- что такое синхронизация - зачем
- синхронизация по this
- статический метод синхронизации
